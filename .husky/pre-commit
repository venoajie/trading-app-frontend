
#!/bin/sh
set -e

# This script runs in a pure Linux/WSL environment.
# Git hooks use a non-interactive shell, so we must manually source nvm.sh
# to make node and npm available in the script's PATH.
if [ -f "$HOME/.nvm/nvm.sh" ]; then
  . "$HOME/.nvm/nvm.sh"
fi

npm run lint-staged